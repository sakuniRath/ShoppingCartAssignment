import { PaymentConfirmation } from './payment-confirmation.model';

describe('PaymentConfirmation', () => {
  it('should create an instance', () => {
    expect(new PaymentConfirmation()).toBeTruthy();
  });
});
